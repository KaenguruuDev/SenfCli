services:
  senfcli:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /work
    container_name: senf-cli
    stdin_open: true
    tty: true
    environment:
      SENF_LOG: "1"
    command: ["sleep", "infinity"]
